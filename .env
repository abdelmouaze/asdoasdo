# Vite environment variables for the FRONTEND
# Copy this file to `.env.local` for local development, or configure these in your Vercel project settings.
# Note: Vite only exposes variables that start with VITE_

# Public base URL of your backend API (must include scheme: http or https)
# Example for local dev (when backend runs on your machine):
# VITE_API_URL=http://localhost:3000
# Example for production (deployed backend URL):
# VITE_API_URL=https://your-backend.example.com
VITE_API_URL=http://localhost:3000

# Optional: App display name
VITE_APP_NAME=Shadow Vortex

# Optional: Environment label (development | staging | production)
VITE_ENV=development
